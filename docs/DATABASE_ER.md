# Database Entity-Relationship Model

Full DDL: [`database/01_Schema.sql`](../database/01_Schema.sql). Identity tables
(`AspNetUsers`, `AspNetRoles`, `AspNetUserRoles`, ...) are generated by EF Core migrations, not
hand-scripted, to stay in lockstep with the installed `Microsoft.AspNetCore.Identity` version.

```mermaid
erDiagram
    Regions ||--o{ Branches : "has"
    Regions ||--o{ AtmMaster : "has"
    Branches ||--o{ AtmMaster : "has"
    AtmMaster ||--o{ AtmTransactions : "records"
    AtmMaster ||--o{ AtmCashLoads : "receives"
    AtmMaster ||--o{ AtmStatusHistory : "tracks"
    AtmMaster ||--o{ ForecastResults : "forecasted for"
    AtmMaster ||--o{ ForecastHistory : "archived for"
    AtmMaster ||--o{ ReplenishmentRecommendations : "recommended for"
    AtmMaster ||--o{ Alerts : "raised for"
    ForecastResults ||--o{ ReplenishmentRecommendations : "backs"
    ReplenishmentRecommendations ||--o| AtmCashLoads : "fulfilled by"
    Alerts ||--o{ NotificationLogs : "dispatched via"
    Regions ||--o{ HolidayMaster : "regional holiday"
    AspNetRoles ||--o{ RolePermissions : "granted"
    Permissions ||--o{ RolePermissions : "granted to"

    Regions {
        int Id PK
        string RegionCode UK
        string RegionName
    }
    Branches {
        int Id PK
        string BranchCode UK
        int RegionId FK
    }
    AtmMaster {
        int Id PK
        string AtmCode UK
        string TerminalId UK
        int RegionId FK
        int BranchId FK
        decimal Capacity
        decimal SafetyBufferAmount
        tinyint Status
    }
    AtmTransactions {
        int Id PK
        int AtmId FK
        date TransactionDate
        decimal WithdrawalAmount
        decimal RemainingCash
        guid ImportBatchId
    }
    ForecastResults {
        int Id PK
        int AtmId FK
        guid ForecastRunId
        date TargetDate
        decimal ForecastedWithdrawalAmount
        decimal ConfidenceScore
    }
    ForecastHistory {
        bigint Id PK
        int AtmId FK
        date TargetDate
        decimal AbsolutePercentageError
    }
    ReplenishmentRecommendations {
        int Id PK
        int AtmId FK
        int ForecastResultId FK
        decimal RecommendedLoadAmount
        tinyint RiskLevel
        date ProjectedDepletionDate
        bit IsFulfilled
    }
    AtmCashLoads {
        int Id PK
        int AtmId FK
        int RecommendationId FK
        decimal AmountLoaded
    }
    Alerts {
        int Id PK
        int AtmId FK
        tinyint AlertType
        tinyint Severity
        tinyint Status
    }
    NotificationLogs {
        int Id PK
        int AlertId FK
        tinyint Channel
        tinyint Status
    }
    AuditLogs {
        bigint Id PK
        datetime2 TimestampUtc
        tinyint Action
        string EntityName
    }
```

## Notable constraints

- `AtmTransactions` has a unique constraint on `(AtmId, TransactionDate)` — one aggregated row
  per ATM per day; see [ARCHITECTURE.md](ARCHITECTURE.md) for why.
- All foreign keys use `ON DELETE NO ACTION` (`Restrict` in EF Core) except
  `NotificationLogs → Alerts`, which cascades — deleting an alert's notification trail with it
  is safe; deleting master/transactional data by cascade is not (financial data integrity).
- `AuditLogs` has no soft-delete column and the application never issues `UPDATE`/`DELETE`
  against it — see `usp_ArchiveOldAuditLogs` for the retention-reporting (not deleting) pattern.
